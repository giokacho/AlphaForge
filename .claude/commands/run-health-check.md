# Run Health Check
Runs the AlphaForge health check and summarizes any failures.

```bash
cd /path/to/AlphaForge && python health_check.py
```

Parse the output and report:
- Which components passed
- Which failed and why
- Recommended fix for each failure
